# 🚀 Realix OS — Полный Отчёт о Разработке и Чейнджлог

В данном документе приведено подробное описание всех архитектурных улучшений, системных вызовов, подсистем и приложений, разработанных для операционной системы Realix OS.

---

## ⚙️ 1. Архитектура Ядра и Защищённый Режим (Kernel32)

### 🛠️ Исправление фатального сбоя Ring 3 (`iretw` vs `iretd`)
- **Проблема:** В файле `source/kernel32/src/rlx_loader.rs` инлайн-ассемблер Rust `"iret"` компилировался в 16-битную инструкцию `iretw` (`66 cf`), которая снимала со стека 16-битный IP и 16-битный CS. Это приводило к сбою `#GP(0x40)` (General Protection Fault) при передаче управления из Ring 0 в Ring 3.
- **Решение:** Инструкция заменена на явно выраженную 32-битную `"iretd"` (`cf`), корректно считывающую 32-битные `EIP`, `CS`, `EFLAGS`, `ESP` и `SS`.

### 🛡️ Изоляция Стека и Механизм Возврата (`setjmp`/`longjmp`)
- **Изоляция стеков:** Стек прерываний Ring 0 в TSS (`0x00088000`) полностью отделен от стека выполнения консольной оболочки ядра (`0x00090000`), предотвращая порчу кадров вызова.
- **Возврат из приложений (`SYS_EXIT`):** Реализовано сохранение контекста ядра (`KERNEL_SAVED_ESP`, `KERNEL_SAVED_EBP`, `KERNEL_SAVED_EIP`). Вызов `SYS_EXIT` через `exit_to_kernel()` совершает прямой переход в цикл команд оболочки `Realix >>`.
- **Расширение GDT:** Таблица дескрипторов сегментов GDT расширена до 16 слотов в `source/kernel32/src/x86/gdt.rs`.

---

## 🔌 2. Спецификация Системных Вызовов (System API `INT 0x80`)

| № | Название вызова | Регистры / Параметры | Описание |
|---|---|---|---|
| **1** | `SYS_PRINT_STRING` | `ESI` = указатель на строку | Вывод нуль-терминированной строки на экран VGA |
| **2** | `SYS_PUTCHAR` | `EDX` = символ, `EBX` = цвет | Вывод символа с VGA-атрибутами цвета (`1`=LightBlue, `2`=LightGreen, `7`=LightGray и др.) |
| **3** | `SYS_EXIT` | *Нет* | Завершение процесса и чистый возврат в ядро |
| **4** | `SYS_READ_KEY` | Возврат `EAX` = символ | Ожидание и считывание нажатия клавиши клавиатуры |
| **5** | `SYS_CLEAR` | *Нет* | Очистка текстового буфера экрана и сброс курсора |
| **6** | `SYS_GET_SYSINFO` | `EDI` = `&RealixSysInfo` | Живой запрос данных системы (процессор `CPUID`, аптайм PIT, память) |
| **7** | `SYS_EXEC_16` | `ESI` = имя файла | Вызов подсистемы исполнения 16-битных приложений из 32-битного режима |

---

## 🌐 3. Переменные Окружения и Диспатчер Команд (PATH Resolver)

- **Глобальная переменная `PATH`:**
  - В ядро и оболочку `source/kernel32/src/shell.rs` внедрена переменная окружения `PATH=/bin;/apps;/`.
  - Добавлена новая команда **`set`** (`env` / `export`) для просмотра и динамической смены `PATH` из консоли (`set PATH=/apps`).
- **Автоматический PATH Resolver:**
  - В `source/kernel32/src/commands/exec.rs` реализована функция `find_and_run`.
  - Программы из путей `PATH` можно запускать напрямую по их именам (`snake`, `rlxfetch`, `rlxfetch16`, `exec16`, `app32`) без необходимости вводить `exec` или `.rlx`.

---

## 📦 4. Созданные и Портированные Приложения (.RLX)

### 1. 🐍 `snake32.rlx` (Игра «Змейка» в Ring 3)
- Написана на ассемблере NASM (`source/apps/snake32.asm`).
- Полноценная консольная игра с обработкой коллизий со стенами, спавном яблок, подсчетом очков, управлением WASD/Q и отрисовкой через `INT 0x80`.

### 2. ⚡ `rlxfetch.rlx` (Нативный Си-порт FastFetch)
- Написана на языке **C11** (`source/apps/rlxfetch.c`) и скомпилирована компилятором **GCC 15** (`gcc -m32 -ffreestanding -nostdlib -fno-pic -fno-pie`).
- Совершает живой запрос системной информации `SYS_GET_SYSINFO` (`INT 0x80`, `EAX = 6`).
- Выводит яркий синий ASCII-логотип **R**, реального вендора CPU через аппаратный `CPUID`, аптайм таймера PIT, память и переменную `PATH`.

### 3. 🖥️ `rlxfetch16.rlx` (16-битная Ассемблерная версия)
- Написана на NASM 16-bit Real Mode (`source/apps/rlxfetch16.asm`).
- Выводит синий логотип **R** через BIOS `INT 0x10`, отображает состояние 16-битного ядра и переменную `PATH`.

### 4. 🌁 `exec16.rlx` (32-битная Си-утилита Моста Подсистемы)
- Написана на Си (`source/apps/exec16.c`) и скомпилирована **GCC 15**.
- Выполняет системный вызов `SYS_EXEC_16` (`INT 0x80`, `EAX = 7`) для переключения контекста и запуска 16-битных бинарников.

### 5. 🌐 Universal Executable Spec v2.0
- Спецификация универсального заголовка бинарника (`RLX_MODE_UNIVERSAL` / `'U'`), содержащего точки входа одновременно для 16-битного и 32-битного режимов.

---

## 🛠️ 5. Сборка, QEMU и Git

- **Makefile:** Настроены правила для компиляции всех бинарников, сборки Си-кода (`entry32.s`, `linker32.ld`) и упаковки в образ `realix.iso`.
- **Нативный QEMU Windows:** Создан скрипт запуска [run-windows.bat](file:///c:/Users/Ramix/Downloads/Realix-latest/run-windows.bat) для плавной работы QEMU без зависимостей от WSLg.
- **Git:** Все изменения сохранены в локальном Git-репозитории проекта.
